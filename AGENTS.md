# Agent instructions — signal-forge

You **MUST** read AGENTS.md at `github:ligoldragon/lore` — the workspace contract.

## Repo role

Layered protocol crate atop signal. Carries the criome ↔ forge wire (Build, Deploy, store-entry operations). Skeleton-as-design today; bodies are `todo!()`.
